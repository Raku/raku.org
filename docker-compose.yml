version: "3.8"

services:
  webapp:
    image: quay.io/chroot.club/proto-25:latest
    container_name: my_webapp
    expose:
      - "4000"
    networks:
      - webnet
    restart: unless-stopped

  caddy:
    image: caddy:latest
    container_name: caddy_proxy
    ports:
      - "80:80"
      - "443:443"
      - "4000:4000"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - webapp
    networks:
      - webnet
    restart: unless-stopped

volumes:
  caddy_data:
  caddy_config:

networks:
  webnet:
